# Contracts — (vide)

Aucune interface externe exposée par cette Epic : pas d'API REST, pas de schéma
d'événement, pas de contrat UI. La vue `dim_dbx_workspace` est le seul « contrat
de données » et il est décrit dans [../data-model.md](../data-model.md).
