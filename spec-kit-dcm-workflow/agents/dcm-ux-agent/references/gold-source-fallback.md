# Confirmed Gold Source Fallback

For the current Compute Monitoring mockup execution, the Product Owner explicitly confirmed:

- authoritative source: `docs/spike/compute-metrics-definition/compute_datamodel.md`
- accepted tables: the nine `gold_dbx_compute_*` tables listed by the Product Owner in the conversation
- column authority: only columns documented in `compute_datamodel.md`

This is an execution-specific confirmation. The canonical `reference/gold-tables-confirmed.md` remains preferred when available. The agent must record this fallback path and confirmation in `gap-analysis.md`.
